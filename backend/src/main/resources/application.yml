spring:
  application:
    name: PersonalMoneyManagement
  
  datasource:
    url: jdbc:mysql://localhost:3306/moneyflow?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Fubo979620!
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      timeout: 60000

server:
  port: 8080

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

jwt:
  secret: mySecretKeyForJwtTokenGenerationThatIsAtLeast256BitsLongForHS256Algorithm
  expiration: 86400000

cors:
  allowed-origins: http://localhost:4200,http://localhost:3000

python:
  service:
    url: http://localhost:8000

logging:
  level:
    com.jason.personalmoneyflow: DEBUG
    org.springframework.security: DEBUG
